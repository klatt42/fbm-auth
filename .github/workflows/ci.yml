name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  ci:
    uses: klatt42/fbm-auth/.github/workflows/python-ci.yml@master
    with:
      working-directory: .
      has-frontend: false
